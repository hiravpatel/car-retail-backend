enum RoleType {
    ADMIN,
    STAFF,
    CUSTOMER
}

enum VehicleStatus {
    AVAILABLE,
    RENTED,
    SOLD,
    MAINTENANCE
}

enum BookingStatus {
    PENDING,
    CONFIRMED,
    CANCELLED,
    COMPLETED
}

enum PaymentMethod {
    CASH,
    CARD,
    ONLINE
}

enum PaymentStatus {
    PENDING,
    PAID,
    FAILED,
    REFUNDED
}

enum MaintenanceStatus {
    ONGOING,
    COMPLETED
}

enum ReportType {
    DAILY,
    WEEKLY,
    MONTHLY
}
